# generated by perl script

NASM = ../../buildtools/nasm/nasm.exe
NASM_FLAGS = -f win32 -g -F cv8

all: all_objs

clean:
	rm -rf *.obj *.asm

generate:
	perl generate.pl

all_objs: \
	InitOnceExecuteOnce.obj \
	inet_pton.obj \

InitOnceExecuteOnce.obj: InitOnceExecuteOnce.asm
	${NASM} ${NASM_FLAGS} InitOnceExecuteOnce.asm -o $@

inet_pton.obj: inet_pton.asm
	${NASM} ${NASM_FLAGS} inet_pton.asm -o $@

