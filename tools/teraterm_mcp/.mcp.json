{
  "mcpServers": {
    "teraterm-mcp": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/start-mcp-server",
      "args": [],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/src"
      }
    }
  }
}
